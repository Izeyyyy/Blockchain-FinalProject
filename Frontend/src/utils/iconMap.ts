import {
  FileBadge,
  FileSearch,
  History,
  LayoutDashboard,
  ShieldCheck,
} from 'lucide-react'

export const iconMap = {
  dashboard: LayoutDashboard,
  history: History,
  shield: ShieldCheck,
  verifyCertificate: FileBadge,
  verifyDocument: FileSearch,
}
